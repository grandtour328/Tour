[package]
name = "vaglolap_torlo"
version = "0.1.0"
edition = "2024"

[dependencies]
windows-sys = { version = "0.52", features = ["Win32_System_DataExchange", "Win32_Foundation"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
